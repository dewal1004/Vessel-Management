OBJECT Table 830 DO Payment Card Type
{
  OBJECT-PROPERTIES
  {
    Date=20120907D;
    Time=120000T;
    Version List=NAVW17.00;
  }
  PROPERTIES
  {
    OnInsert=VAR
               DOPaymentCardType@1000 : Record "DO Payment Card Type";
             BEGIN
               if "Sort Order" = 0 then begin
                 DOPaymentCardType.SetCurrentKey("Sort Order");
                 if DOPaymentCardType.FindLast then
                   "Sort Order" := DOPaymentCardType."Sort Order" + 1
                 else
                   "Sort Order" := 1;
               end;
             END;

    CaptionML=ENU=Dynamics Online Payment Card Type;
    LookupPageID="Workflow Webhook Entries";
    DrillDownPageID="Workflow Webhook Entries";
  }
  FIELDS
  {
    { 1   ;   ;Sort Order          ;Integer       ;AutoIncrement=false;
                                                   CaptionML=ENU=Sort Order }
    { 2   ;   ;Name                ;Code20        ;CaptionML=ENU=Name;
                                                   NotBlank=true }
    { 3   ;   ;Validation Rule     ;Integer       ;CaptionML=ENU=Validation Rule;
                                                   NotBlank=true }
    { 4   ;   ;Numeric Only        ;Boolean       ;InitValue=true;
                                                   CaptionML=ENU=Numeric Only }
    { 5   ;   ;Allow Spaces        ;Boolean       ;InitValue=true;
                                                   CaptionML=ENU=Allow Spaces }
    { 6   ;   ;Min. Length         ;Integer       ;CaptionML=ENU=Min. Length }
    { 7   ;   ;Max. Length         ;Integer       ;CaptionML=ENU=Max. Length }
  }
  KEYS
  {
    {    ;Name                                    ;Clustered=true }
    {    ;"Sort Order"                             }
  }
  FIELDGROUPS
  {
  }
  CODE
  {

    [LineStart(50703)]
    PROCEDURE CreateDefaults@1();
    BEGIN
      if not FindFirst then begin
        Init;
        "Sort Order" := 1;
        Name := 'VISA';
        "Validation Rule" := 1;
        "Numeric Only" := true;
        "Allow Spaces" := false;
        "Min. Length" := 16;
        "Max. Length" := 16;
        Insert;

        Init;
        "Sort Order" := 2;
        Name := 'MASTER CARD';
        "Validation Rule" := 1;
        "Numeric Only" := true;
        "Allow Spaces" := false;
        "Min. Length" := 16;
        "Max. Length" := 16;
        Insert;

        Init;
        "Sort Order" := 3;
        Name := 'AMERICAN EXPRESS';
        "Validation Rule" := 1;
        "Numeric Only" := true;
        "Allow Spaces" := false;
        "Min. Length" := 15;
        "Max. Length" := 15;
        Insert;

        Init;
        "Sort Order" := 4;
        Name := 'DISCOVER';
        "Validation Rule" := 1;
        "Numeric Only" := true;
        "Allow Spaces" := false;
        "Min. Length" := 16;
        "Max. Length" := 16;
        Insert;
      end;
    END;

    BEGIN
    END.
  }
}

